meta-ads-dashboard/
├── README.md           ← documentação principal (já criado)
├── .gitignore          ← protege credenciais (já criado)
├── worker/
│   └── worker.js       ← código do Worker (já criado)
├── generator/
│   └── index.html      ← gerador de dashboards (artifact "Gerador")
├── template/
│   └── index.html      ← template base (artifact "GIO — index.html v5")
└── docs/
    ├── setup.md        ← guia passo a passo (já criado)
    └── faq.md          ← perguntas frequentes (já criado)
